version: "3.8"

services:

  postgres:
    image: postgres:15
    container_name: jarvis-postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: jarvis
      POSTGRES_PASSWORD: jarvis
      POSTGRES_DB: jarvis